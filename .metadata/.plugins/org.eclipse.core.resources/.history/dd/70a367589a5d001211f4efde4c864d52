package com.vectorone;

import com.data.DataClass;

import android.content.Context;
import android.graphics.Canvas;
import android.graphics.Color;
import android.graphics.Paint;
import android.view.View;

public class RadarTargetOverlay extends View {

	public RadarTargetOverlay(Context context) {
		super(context);
		// TODO Auto-generated constructor stub
	}

	@Override
	protected void onDraw(Canvas canvas) {
		// TODO Auto-generated method stub
		Paint paint = new Paint();
		paint.setStyle(Paint.Style.FILL);
		paint.setColor(Color.RED);

		for (int i = 0; i < DataClass.cachs.size(); i++) {
			canvas.drawCircle(calculatex(DataClass.cachs.get(i)), calculatey(DataClass.cachs.get(i), 10, paint)
		}

		super.onDraw(canvas);
	}

}
